{
  "name": "maxtourvietnam",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:tours": "node scripts/sync-tour-content.mjs",
    "build": "npm run sync:tours && npm run typecheck && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.worker.json",
    "preview": "vite preview",
    "cf:dev": "wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply maxtourvietnam --local",
    "db:migrate:remote": "wrangler d1 migrations apply maxtourvietnam --remote",
    "db:seed:local": "wrangler d1 execute maxtourvietnam --local --file=seed/tours.sql",
    "db:seed:remote": "wrangler d1 execute maxtourvietnam --remote --file=seed/tours.sql"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "cheerio": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}
